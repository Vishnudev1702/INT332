Commands Used
touch docker-compose.yaml
open -e docker-compose.yaml

docker compose up -d
docker compose config --services

docker exec -it postgres-db psql -U postgres

# Docker Compose Practice – 2026-04-02

## docker-compose.yaml

```yaml
services:
  frontend:
    image: node:18
    container_name: frontend-app
    working_dir: /app
    volumes:
      - ./frontend:/app
    ports:
      - "3000:3000"
    command: sh -c "npm install && npm start"
    depends_on:
      - backend

  backend:
    image: eclipse-temurin:17
    container_name: backend-app
    working_dir: /app
    volumes:
      - ./backend:/app
    ports:
      - "8080:8080"
    command: java -jar app.jar
    environment:
      DB_HOST: db
      DB_PORT: 5432
      DB_NAME: mydb
      DB_USER: postgres
      DB_PASSWORD: password
    depends_on:
      - db

  db:
    image: postgres:15
    container_name: postgres-db
    restart: always
    environment:
      POSTGRES_DB: mydb
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: password
    volumes:
      - db_data:/var/lib/postgresql/data
    ports:
      - "5432:5432"

volumes:
  db_data: